import type { HeadingProps, TextProps } from "@chakra-ui/react";

export const defaultSubheadingStyles: HeadingProps = {
  fontSize: "12px",
  fontWeight: "800",
  width: "fit-content",
};

export const defaultSideLabelStyles: TextProps = {
  fontWeight: "700",
  fontSize: "9px",
  transform: "rotate(90deg)",
};

export const defaultBdayData = { month: 1, day: 1, year: 2000 };
